import { PLUS, MINUS, ZERO } from '../constants';


export const PlusAction = () => ({ type: PLUS });

export const MinusAction = () => ({ type: MINUS });

export const ZeroAction = () => ({ type: ZERO });